-- This file is for plugins that don't require a lot of config, so don't
-- need a special file.

return {
  "nvim-lua/plenary.nvim",
  "christoomey/vim-tmux-navigator", -- This allows Ctrl-H,J,K,L to move between split panes.
}
